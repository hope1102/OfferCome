# OfferCome 产品与开发需求

> 本文档是 OfferCome 的长期交接文档。后续 Agent 或开发者应先阅读本文档，再查看 `README.md`、`manifest.json` 和 `src/`。

## 1. 产品定位

OfferCome 是一个面向个人使用的 Edge/Chromium 网申简历自动填写扩展。用户只需在扩展中维护一份结构化简历，进入企业招聘官网或 ATS（Applicant Tracking System）页面后，点击“一键填写”，扩展即可识别表单字段并将对应简历内容写入页面。

产品不设置填写次数、网站数量或简历字段数量限制。默认本地运行、本地存储，不依赖 OfferCome 自建服务器，不自动提交申请，也不绕过验证码、短信验证或招聘网站的安全措施。

## 2. 核心目标

1. 一次录入，多站复用：用户只维护一份标准化资料。
2. 通用识别：在没有网站专用规则时，也能依据标签、占位文字、字段名、ARIA、上下文和分组标题识别字段。
3. 专站增强：针对常见招聘系统维护适配器，提高复杂组件和重复经历的填写成功率。
4. 可控填写：填写前可以扫描和预览；只填高置信度字段；填写后报告成功、跳过和失败项目。
5. 本地优先：敏感简历信息默认只存储于浏览器 `chrome.storage.local`。
6. 可持续维护：字段字典、网站规则、填充引擎和 UI 分层，便于后续 Agent 独立扩展。

## 3. 非目标与安全边界

- 不自动点击“提交申请”“确认投递”等最终提交按钮。
- 不破解验证码、滑块、短信验证、登录限制或反自动化机制。
- 不伪造用户经历，不在没有明确资料依据时生成事实性回答。
- 不复制闭源竞品代码；只能参考其公开功能表现，或复用许可证明确允许的开源代码。
- 浏览器禁止脚本直接指定本地文件路径，因此简历附件上传通常需要用户在文件选择器中确认；扩展应识别并提示文件字段。
- 对封闭 Shadow DOM、跨域 iframe、Canvas 表单等无法访问的控件，应明确报告而不是假装成功。

## 4. 用户流程

### 4.1 首次配置

1. 用户安装未打包扩展。
2. 打开“简历资料”页面。
3. 手动填写结构化资料，或导入 OfferCome JSON。
4. 保存到浏览器本地。
5. 可导出 JSON 作为备份或在其他设备迁移。

### 4.2 网申填写

1. 用户打开招聘网站的简历/申请表页面。
2. 点击浏览器工具栏中的 OfferCome 图标，打开常驻侧栏。
3. 点击“扫描页面”，查看识别数量、候选值、置信度和未匹配字段。
4. 点击“一键填写”。
5. 扩展填写可安全处理的字段，并显示结果统计。
6. 用户检查低置信度、未匹配、附件和验证码字段，手工补充后自行提交。
7. 一键填写后自动创建或更新申请记录，状态默认为“已填写”；只有用户确认后才改为“已投递”。

### 4.3 申请进度管理

1. 工作台按“已收藏、已填写、已投递、筛选沟通、测评/笔试、面试中、Offer”展示进行中看板。
2. 用户可拖拽卡片或在详情中调整状态；未通过和主动撤回进入已结束视图。
3. 详情保存职位、公司、地点、原链接、职位摘要、备注、下一步日期和状态时间线。
4. 同一规范化职位链接重复填写时更新原记录，不重复创建卡片，也不回退已有的后续状态。
5. 申请记录支持归档、删除和 JSON 导出，全部使用浏览器本地存储。

## 5. 简历数据模型

数据模型需要版本化。当前版本为 `schemaVersion: 1`。

### 5.1 基本信息 `basics`

- 中文姓名、英文名、曾用名
- 性别、出生日期、年龄、民族、政治面貌、婚姻状态
- 手机号、备用手机号、邮箱、备用邮箱
- 身份证/证件类型/证件号（敏感字段，默认不主动填写低置信度目标）
- 国籍、籍贯、出生地、户口所在地、现居住地
- 省、市、区、详细地址、邮编
- 微信、QQ
- 个人网站、GitHub、LinkedIn、作品集
- 期望城市、期望职位、期望薪资、可入职日期
- 个人简介、自我评价、优势、兴趣爱好

### 5.2 教育经历 `education[]`

- 学校、学院、专业、学历、学位
- 入学日期、毕业日期、是否在读、是否统招
- GPA、GPA 满分、排名、排名总人数
- 主修课程、研究方向、导师、教育描述

### 5.3 工作/实习经历 `experience[]`

- 公司、部门、职位、类型（全职/实习/兼职）
- 工作城市、开始日期、结束日期、是否至今
- 工作职责、主要成果、离职原因

### 5.4 项目经历 `projects[]`

- 项目名称、角色、开始日期、结束日期
- 项目链接、技术栈、项目描述、职责、成果

### 5.5 校园/社团经历 `campus[]`

- 是否有担任学生干部经历、干部级别、干部职级、开始日期、结束日期、工作描述

### 5.6 技能、语言、证书、奖项

- `skills[]`：名称、熟练度、描述
- `languages[]`：语言、等级、分数、听说读写水平
- `certificates[]`：证书名称、颁发机构、日期、编号
- `awards[]`：奖项名称、级别、日期、描述

### 5.7 家庭成员 `family[]`

- 姓名、关系、工作单位、职务、联系电话

### 5.8 论文与专利 `publications[]`

- 类型：论文、发明专利、实用新型专利、外观专利、软件著作权或其他
- 论文/专利名称、发表/授权渠道、作者顺序
- 影响因子、专利号、发表/授权时间
- 论文/专利链接和详情
- 支持多段记录，并与项目、奖项和证书章节严格隔离

### 5.9 求职问题 `answers`

键值结构，支持用户维护常见开放问题，例如：

- 为什么选择本公司/岗位
- 职业规划
- 优势与不足
- 是否接受调剂/加班/出差
- 是否有亲属在本公司
- 信息来源

## 6. 页面字段识别

### 6.1 可识别控件

- `input`：text、email、tel、number、date、month、radio、checkbox
- `textarea`
- 原生 `select`
- `contenteditable`
- 常见 UI 框架的可访问自定义输入和下拉组件
- 文件控件只识别和提示，不强制写入

### 6.2 字段文本采集

候选文本至少包含：

- `<label for>`、父级 label
- `name`、`id`、`placeholder`、`title`
- `aria-label`、`aria-labelledby`
- 控件相邻文本
- 表单项容器内标签
- fieldset legend、分组标题、折叠面板标题
- 所在重复经历的序号和类型

### 6.3 匹配策略

按优先级组合打分：

1. 网站专用规则精确匹配。
2. 字段别名的完整匹配。
3. 标准化后的包含匹配。
4. 多 token 交集匹配。
5. 控件类型、章节类型、日期语义和排除词加减分。
6. 用户保存的站点纠正规则。
7. 可选 AI 兜底（未来能力，必须由用户自行配置服务和密钥）。

置信度建议：高 `>= 0.82`，中 `0.62-0.81`，低 `< 0.62`。自动填写默认仅执行中高置信度匹配；身份证、证件号等敏感字段要求更高阈值。

## 7. 填充引擎

- 使用原生属性 setter 写值，兼容 React/Vue/Angular 受控输入。
- 依次触发 `focus`、`input`、`change`、`blur` 等事件。
- 下拉框按照标准化后的文本和值选择，而非只按索引。
- radio/checkbox 根据 label、value、布尔语义选择。
- 日期支持 `YYYY-MM-DD`、`YYYY-MM`、中文年月等格式转换。
- 不覆盖用户已有值，除非用户显式启用“覆盖已有内容”。
- 填写完成后再次读取控件值，区分“成功”“被页面回滚”“失败”。
- 对自定义下拉框采用插件化处理，避免通用逻辑误点提交按钮。
- 密码框、只读字段和超出用户资料数量的重复经历不参与填写。
- 候选字段或下拉选项存在歧义时宁可跳过，不按 DOM 顺序猜测。

## 8. 重复经历处理

教育、实习、工作、项目、校园经历通常需要先点击“新增”。完整目标是：

1. 识别页面已有经历卡片。
2. 将第 N 个页面卡片映射到简历数组第 N 项。
3. 数量不足时，在用户确认后点击对应“新增”按钮。
4. 新增 DOM 稳定后继续填充。
5. 防止误点“删除”“保存”“提交”。

首版可以先填写页面中已经存在的经历卡片，并为自动新增保留适配器接口。

## 9. 网站适配范围

优先级：

1. 通用 HTML 表单。
2. Moka。
3. 北森/Beisen。
4. 飞书招聘。
5. 用友大易。
6. 主流公司自研系统：腾讯、阿里、字节、京东、美团、百度、网易、快手、华为、OPPO、vivo、比亚迪等。

适配器应以域名、页面特征和组件特征判断，不能只依赖域名。适配器只覆盖差异点，通用逻辑仍由核心引擎负责。

## 10. UI 要求

### 10.1 常驻侧栏

- 点击网页输入框时保持打开并实时显示当前目标
- 按基本信息、求职期望、教育/工作/项目等模块导航或搜索简历资料，并定点填写最后选中的控件
- 当前站点和识别状态
- 扫描页面
- 一键填写
- 覆盖已有值开关
- 高/中/低置信度统计
- 填写成功、跳过、失败统计
- 打开简历编辑器

### 10.2 简历编辑器

- 分区编辑基本资料和多段经历
- 自动保存或明确保存反馈
- JSON 导入、导出和恢复默认模板
- 敏感字段提示
- 数据仅本地存储提示

### 10.3 页面反馈

- 成功填写字段短暂绿色高亮
- 未匹配必填字段黄色高亮
- 失败字段红色高亮
- 不遮挡招聘页面核心操作

### 10.4 求职工作台

- 进行中、已结束和已归档三种视图
- 按职位、公司、地点搜索并按状态筛选
- 看板拖拽流转与详情侧栏编辑
- 状态时间线、个人备注和下一步日期
- 自动记录方式、所用资料、最近填写统计与投递复盘总结
- 打开原职位、归档、删除和申请记录导出

## 11. 权限与隐私

- 使用 Manifest V3。
- 权限最小化：`storage`、`activeTab`、`scripting`；仅在确有必要时增加。
- 内容脚本需要在用户打开的网申页面执行，因此首版使用 `<all_urls>` host 权限；文档中明确说明原因。
- 不采集浏览历史，不上传页面内容，不内置遥测。
- 将来增加 AI 服务时，必须明确显示将发送的字段和目标服务，并默认关闭。

## 12. 技术结构

- `manifest.json`：MV3 清单。
- `src/shared/`：数据模型、存储、字段字典、通用工具。
- `src/content/`：页面分析、字段匹配、填充引擎、网站适配器。
- `src/popup/`：常驻侧栏、扫描和填写入口（沿用早期目录名）。
- `src/options/`：结构化简历编辑器。
- `src/background/`：安装初始化和消息协调。
- `tests/`：匹配、格式转换和填充行为测试。

首版采用原生 HTML/CSS/JavaScript，不要求 npm 构建，可直接在 Edge 中加载目录。后续若引入 TypeScript/Vite，必须保留一条清晰的构建和打包命令。

## 13. 第一版验收标准

- 扩展可在 `edge://extensions` 以“加载解压缩扩展”方式安装。
- 用户可编辑、保存、导入、导出结构化简历。
- 扩展刷新后资料仍然存在。
- 能扫描普通 HTML 表单并列出匹配字段和置信度。
- 能填写文本、邮箱、电话、数字、textarea、select、radio、checkbox、date 和 contenteditable。
- 不覆盖已有内容的默认行为有效。
- React 风格受控输入能收到 input/change 事件。
- 不自动提交表单。
- 填写后展示成功、跳过、失败、未匹配数量。
- 代码不依赖远程服务器即可运行。

## 14. 后续路线

### Phase 2

- 可视化逐字段预览与人工改绑。
- 保存站点纠正规则。
- 自动新增重复经历。
- Moka、北森、飞书、用友大易专项适配和回归样例。
- Word/PDF 简历解析后映射到数据模型。

### Phase 3

- 可选的 BYOK AI 字段语义兜底。
- 多份简历与职位定向版本。
- 开放问题答案库和基于真实经历的辅助草拟。
- 更多统计图表、面试记录与提醒能力。
- 适配规则的离线导入导出。

## 15. 开发约定

- 禁止把真实简历、手机号、证件号或 API Key 提交到 Git。
- 新增字段时同步更新 schema、编辑器、别名字典和测试。
- 网站适配修复应尽可能附最小化脱敏 HTML fixture。
- 所有自动点击行为必须使用按钮文案白名单，并排除提交、删除、确认投递等危险动作。
- 未验证成功的填写不得计入成功数。
- 每次交接更新本文档末尾的开发进度。

## 16. 开发进度

- 2026-09-17：完成需求定义。
- 2026-09-17：完成 Manifest V3 第一版：本地简历编辑、JSON 导入导出、页面扫描、置信度匹配、文本/日期/select/radio/checkbox/contenteditable/常见 combobox 填充、结果高亮与统计。
- 2026-09-17：增加脱敏通用网申回归页；真实 Chromium 回归通过 16 个控件，覆盖两段重复教育经历和 input/change 事件。
- 2026-09-17：修复家庭成员与实习/工作经历双向串填，以及家庭成员姓名错误回退到本人姓名的问题；章节匹配改为隔离式并让明确的局部字段语义优先，真实 Chromium 回归扩展至 18 个控件。
- 2026-09-17：增加内容脚本断线自动恢复；扩展重新加载后，弹窗会在普通网页自动重新注入扫描与填写脚本并重试。
- 2026-09-17：按网申实际字段重构学生会/社团活动，修复校园工作描述与干部级别跨章节串填；增加只替换指定资料、保留其他现有数据的合并导入模式，Chromium 回归扩展至 24 个控件。
- 2026-09-17：实现本地求职工作台。一键填写后按规范化职位链接自动去重记录为“已填写”；支持进行中/已结束/归档看板、拖拽状态流转、详情编辑、时间线、备注、下一步日期、原职位链接和 JSON 导出。
- 2026-09-18：强化自动填写安全边界：修复多余重复经历复用最后一条资料、autocomplete 子串误判、父级文本污染、下拉选项误选及只验证非空等问题；忽略密码和只读字段，写入后严格回读验证。Chromium 脱敏回归通过 28 个控件，其中 24 个安全匹配，第三段无资料教育经历保持空白。
- 2026-09-18：完成“一键填写 → 自动申请记录 → 同职位去重 → 投递复盘”的浏览器完整链路回归；记录详情新增填写方式、使用资料、填写统计、复盘结论和复盘总结，申请记录保存异常不再影响已经完成的表单填写结果。
- 2026-09-18：新增独立论文/专利模块，支持类型、名称、发表/授权渠道、作者顺序、影响因子、专利号、时间、链接和详情，并接入 JSON、本地编辑器和自动填写。Chromium 回归扩展至 37 个控件、32 个安全匹配；新增第二段记录不会覆盖第一段。
- 2026-09-18：完成第一轮北森/iTalent 专项适配：支持字段标题与控件分层的 DOM、自定义 listbox 下拉、籍贯/生源地/学习形式字段，并阻止“国家区号”复合下拉误匹配手机号。北森脱敏 Chromium 回归通过 14 个控件中的 13 个安全匹配，唯一跳过项为国家区号选择器；通用 37 控件回归继续通过。
- 2026-09-18：根据中交北森真实页面回测升级至 0.3.1：收紧双列字段边界，修复姓名输入框被相邻证件号码污染；无法分离姓名/证件号时强制跳过。扩展动态弹层选项与只读日期输入支持，并在弹窗直接列出失败字段。强化后的北森双列/无 ARIA 下拉/只读日期脱敏回归及通用 37 控件回归通过。
- 2026-09-18：根据第二轮北森真实页面回测升级至 0.3.2：将占位文字为“请选择”的伪文本输入识别为自定义下拉，通过 pointerdown/mousedown/click 激活组件，并仅从点击后出现的弹层中选择；对无 option 语义的弹层增加可见叶节点兜底。
- 2026-09-20：升级至 0.4.0：增加带模块导航和搜索的定点填写资料列表；入口改为 Edge/Chromium 原生常驻侧栏，点击网页输入框时侧栏保持打开并实时切换目标，不再受工具栏弹窗失焦关闭影响。

### 当前已知边界

- 北森已根据真实页面截图完成第一轮结构适配，并有脱敏 DOM 回归；由于未取得登录态页面 HTML，北森级联地区、动态新增经历等站点组件仍需结合后续样例迭代。Moka、飞书、用友大易目前仍只有平台识别和通用容器支持。
- 重复经历可以按页面中已有卡片的顺序填写；自动点击“新增经历”仍属于 Phase 2。
- 暂未实现 PDF/Word 简历解析，首版通过编辑器或 OfferCome JSON 导入资料。
- 跨域 iframe、封闭 Shadow DOM、文件附件和验证码仍需用户处理。
- 自定义级联地址、日期弹窗和站点特有富文本组件需要根据脱敏页面样例继续增加适配器。
